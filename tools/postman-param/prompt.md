# 原始 Prompt — TOOL-postman-param（Postman Collection 参数化生成）

> 溯源说明：本文件原样保存生成该工具的原始 Prompt（2026-08-27 用户提供），后续迭代可复现。
> 业务字段均为 `[占位符]`，使用时按实际 Collection 填充后，连同导出的 Collection JSON 一起发给 AI。

---

【角色设定】
你现在是一位资深的接口自动化测试工程师和 Postman 脚本专家。我有一份 Postman Collection 的 JSON 源码，需要你帮我进行“反写”改造，使其支持批量自动化测试。

【核心业务目标】
通过 Postman 的 Collection Runner 自动运行约 [填写迭代次数，如: 250] 次迭代，模拟完整的业务链路，生成大批量测试数据。

【接口串联与数据流转要求】

ID 传递：接口 [A] 返回的 [字段名，如: leadId] 必须被提取，并作为参数传递给后续的接口 [B, C, D]。

数据一致性：接口 [C] 中的 [字段名，如: parent_name] 必须与接口 [A] 中生成的值保持完全一致。

【数据动态自增要求】

手机号自增：接口 [A] 的 mobile 字段需要从 [起始值] 开始，每次迭代自增 1。

姓名自增：接口 [A] 的 parent_name 字段需要从 [起始值，如: Jenny0811-00000] 开始，每次迭代自增 1，并保持 [如: 5位数字] 格式。

【特定参数修改要求】
接口 [B] 中的 [字段名，如: owner_email] 需要固定修改为 [新值，如: autouser251434@test.com]。

【时间滑动要求（如有）】
接口 [E] 的时间参数需要动态计算：

起始时间：[基准时间，如: 2026-08-12T00:00:00.000Z]

滑动步长：每次迭代的时间段需要往后推移 [如: 15分钟]。

【工程化与代码规范要求】

消除提示：必须在所有接口配置测试脚本（Tests），确保 Runner 执行时不会出现 "No tests found" 的警告。

反写 JSON：请直接在我提供的 JSON 源码内部完成所有的 Body 变量替换、脚本注入（Pre-request 和 Tests）以及逻辑串联。

最小化人工配置：除了需要在 Environment 中手动配置的初始变量外，其余所有逻辑均通过代码自动实现。

【环境变量初始化清单要求】
请根据上述需求，为我梳理一份导入 JSON 后需要手工配置的【环境变量清单】。请严格按照以下 Markdown 表格格式输出，以便我直接复制粘贴到 Postman 的 Environment 中：

| 变量名 (Variable) | 初始值 (Initial Value) | 用途说明 |
|--------|--------|----------|
| [变量名] | [初始值] | [该变量在哪个接口、起什么作用] |
| [变量名] | [初始值] | [该变量在哪个接口、起什么作用] |
