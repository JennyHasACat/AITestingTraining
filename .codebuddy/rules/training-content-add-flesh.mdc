---
description: 培训课件持续"加肉"——在指定模块 lecture.md 按标准骨架插入新小节，并自动跑一致性自检防回归。触发词「喵喵喵加肉」；「喵喵喵加肉-工具」走工具入课全链路（tools/ 入库 + toolbox.md 索引 + 选学小节）。
alwaysApply: false
---

# 培训课件加肉规则（training-content-add-flesh）

## 触发

用户消息包含「喵喵喵加肉」时启用。典型输入：
「喵喵喵加肉：在 M6 加一个『AI 自然语言转 SQL 工具』小节，内容是……」
「喵喵喵加肉：M3 补『AI 一键上传测试用例』小工具，思路是 CSV 桥接」

## 执行流程

1. **定位模块**：解析用户指定的模块编号（M1–M10），读 `docs/modules/XX-*/lecture.md`。
2. **确定插入位置**：在最后一个「第 N 节」之后、本模块小结（`## 本模块小结`）之前插入新小节；节号顺延（N+1）。若模块无编号节（极少数），按现有层级追加。
3. **套用骨架**：严格使用以下骨架，用户给的内容填入对应段：

   ```markdown
   ## 第 N 节：[标题]（X min）

   ### 它解决什么问题
   [来自用户输入]

   ### 核心思路 / 提示词骨架
   [来自用户输入；私有内容只写思路]

   ### 使用示例
   [来自用户输入，或留空标注待补]

   ### 注意事项
   [来自用户输入，或默认写：人工 Review 兜底 / 数据安全]
   ```

   若用户明确是"提效小工具"，小节名改为：
   `## 第 N 节：AI 提效小工具 —— [工具名]（X min）` 并加 `> 待补充：...` 标注。

4. **护栏检查（插入后必做）**：
   - 不得新增与现有 `T-/B-/R-/DB-/AUTO-/D-/OP-` 重复的模板编号；
   - 不改动既有小结表、导航链接、quiz 题号；
   - 若新增 Prompt 模板，必须带 M9 标准元数据且编号唯一。
5. **回归自检**：插入完成后，提示用户运行「喵喵喵培训自检」做正式校验（或直接调用该护栏的检查项）。
6. **记录**：若项目存在 HANDOFF 文档，提示将本次加肉追加到对应日期的修复/扩展记录。

## 工具型分支（触发词「喵喵喵加肉-工具」）

用户提供「Python 脚本 + 生成它的原始 Prompt」入课时启用。典型输入：
「喵喵喵加肉-工具：脚本 xxx.py 入 M3，原始 Prompt 如下……」

全链路流程：

1. **资产入库**：建 `tools/<tool-id>/`（语义 ID，风格 `TOOL-xxx`，不用序号，目录名 = ID 后缀），固定三件套：
   - 脚本本体（从用户提供位置移入）
   - `prompt.md`：原样保存用户给的原始 Prompt（溯源，迭代可复现）
   - `README.md`：用途 / 用法 / 边界（课件小节的素材来源）
2. **索引登记**：`docs/toolbox.md` 索引表追加一行（工具 ID / 名称 / 解决什么问题 / 模块小节链接 / 脚本位置）。
3. **课件加节**：按主流程在指定模块 `lecture.md` 小结之前追加小节，标题固定：
   `## 第 N 节：AI 提效小工具 —— [工具名]（10 min，选学）`
   - 工具型小节**一律选学、默认 10 min、不计入模块总时长**（课程总时长 18h 不变，无需改课程表 / mkdocs 描述 / 思维导图）；
   - 小节头部标注：TOOL-xxx、`toolbox.md` 索引链接、脚本仓库路径；
   - 「核心思路 / 提示词骨架」段从原始 Prompt 浓缩关键要素，完整版指向 `tools/<tool-id>/prompt.md`。
4. **文档同步**：首次建 `tools/` 或新增工具条目时，同步 `docs/AI_CONTEXT.md` 目录树。
5. **回归自检 + 记录**：同主流程第 5、6 步；一致性护栏第 ⑦ 项（tools/ ↔ toolbox.md ↔ 课件小节三者一致 + TOOL- 编号唯一）必过。

### 公开 / 私有默认策略

默认公开：纯通用工具、无公司信息即可入库并链接到 GitHub 仓库路径；含敏感实现的只在索引与小节写「思路 + 伪步骤」，脚本留私有仓库。

## 约束

- 课件侧仅修改 `docs/modules/**/lecture.md` 与 `docs/toolbox.md`，不碰 quiz / lab / roadmap（除非用户明确要求）。
- 私有内部代码、链接、密钥一律不写，只写思路与伪步骤。
- 不擅自改变课程的模块数量与命名体系。
- 工具型加肉不改动既有小节编号、模块总时长与 quiz 题号。
