---
description: 培训内容一致性护栏 — 编辑 docs/ 下培训材料或执行「喵喵喵培训自检」时，校验 quiz 题号连续、模板编号唯一、README 数量声明、贡献门槛、ROI 基线、模板引用一致性。
alwaysApply: false
enabled: true
updatedAt: 2026-07-22
provider: codebuddy
---

# 培训内容一致性护栏

## 流程图

```
编辑 docs/ 培训材料 或 触发「喵喵喵培训自检」
              │
              ▼
     ┌─ 逐项检查 ─────────────────────┐
     │  ① quiz 题号连续 + 页头题数一致   │
     │  ② 模板编号全目录唯一            │
     │  ③ README「X+ 条」= 实际模板数    │
     │  ④ 贡献门槛统一为「2 人以上」     │
     │  ⑤ M10 lecture ROI 基线=lab 预估  │
     │  ⑥ 文档引用的模板编号真实存在     │
     └──────────────┬──────────────────┘
                    ▼
            发现不一致 → replace_in_file 修正
```

## 触发条件

- 修改 `docs/modules/**` 或 `docs/prompt-templates/**` 任意文件之后。
- 或用户说「喵喵喵培训自检」时，对全仓库 `docs/` 做一次全量扫描。

## 检查清单

### ① quiz 题号连续

每个 `modules/*/quiz.md`：题目编号必须为 `Q1, Q2, ...` 连续无跳号；页头「题目数量：N 题」必须等于实际题数。发现 `Q5` 直接跳 `Q7` 这类跳号，将后项重编号为 `Q6`（或直接补题），并使页头题数自洽。

### ② 模板编号全局唯一

`prompt-templates/` 下所有 `## [编号]` 的编号（`T-` / `B-` / `R-` / `DB-` / `AUTO-` / `D-` / `OP-` 前缀）在**全目录内**不得重复。若重复，将其中一个重命名为未占用编号（报告 / 文档类优先用 `D-` 前缀）。

### ③ README 数量声明

`prompt-templates/README.md` 顶部「X+ 条」必须 ≥ 实际模板总数（各分类文件编号加总）。虚高则改为真实区间（如「30+ 条」）。同时同步检查 `docs/README.md` 与 `docs/AI_CONTEXT.md` 中的同类声明，三者必须一致。

### ④ 贡献门槛统一

「贡献入库需几人验证」的表述，在 `prompt-templates/README.md` 与 `modules/09-prompt-assets/lecture.md` 中必须一致，统一为「2 人以上 / 2+ 人验证」。禁止出现「1 位以上」之类的矛盾表述。

### ⑤ M10 ROI 基线一致

`modules/10-full-loop-roi/lecture.md` 的「时间效率基线对比」传统方式列，必须与 `lab-generic.md` 的「传统预估」列逐环节一致：

| 环节 | 传统方式 |
|------|---------|
| 需求评审（1 个功能点） | 30 min |
| 用例设计（20 条用例） | 120 min |
| 脚本生成（1 个流程） | 240 min |
| 测试报告（1 次迭代） | 60 min |

### ⑥ 模板引用有效

任何文档中「用模板 T-xx / D-xx / R-xx …」引用的编号，必须在 `prompt-templates/` 中真实存在。重命名编号时，必须同步搜索全 `docs/` 更新所有引用点（含 lecture / lab / `AI_CONTEXT.md` / `README.md` 导航表）。

## 注意

- 本规则只做一致性校验与修正，不新增培训内容。
- 涉及编号重命名时，务必同时更新所有引用，避免留下断链。
