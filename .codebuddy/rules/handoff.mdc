---
description: 工作交接文档自动维护 — 触发「喵喵喵handoff」时，扫描项目未完结信号（git 状态/提交脉络/TODO 标记/既有 handoff），增量更新或新建 HANDOFF 文档，沉淀进行中、阻塞项、下一步与用户偏好，便于随时接手或跨会话延续。
alwaysApply: false
enabled: true
updatedAt: 2026-07-22
provider: codebuddy
---

# 工作交接（Handoff）文档自动维护

## 流程图

```
用户说「喵喵喵handoff」
              │
              ▼
     ┌─ 第1步：定位 handoff 文件 ─────────┐
     │  根目录 HANDOFF-*.md                │
     │   · 命中 1 个 → 直接用              │
     │   · 命中多个 → 选最近修改的          │
     │   · 无命中   → 新建 HANDOFF-<主题>.md│
     └──────────────┬─────────────────────┘
                    ▼
     ┌─ 第2步：采集未完结信号 ─────────────┐
     │  ① git status --short（改了啥未提交）│
     │  ② git log --oneline -10（近期脉络） │
     │  ③ 搜 TODO/FIXME/HACK/暂不/待定/     │
     │     待确认/blocked/WIP              │
     │  ④ 读既有 handoff（保留已确认事实）  │
     └──────────────┬─────────────────────┘
                    ▼
     ┌─ 第3步：增量合并 ──────────────────┐
     │  已确认完成 → 不回退                 │
     │  进行中/阻塞 → 补最新状态            │
     │  下一步 → 按优先级重排              │
     │  偏好/决策 → 同步记录               │
     └──────────────┬─────────────────────┘
                    ▼
     ┌─ 第4步：落盘 + 更新时间戳 ──────────┐
     │  写/更新 HANDOFF-*.md               │
     │  刷新「最后更新」与状态行            │
     └──────────────┬─────────────────────┘
                    ▼
            输出交接摘要给用户
```

## 触发条件

用户消息中包含 **「喵喵喵handoff」**（全角/半角均可识别）即触发。本规则为**触发式**，非 alwaysApply。

## 落点约定

- handoff 文件命名：`HANDOFF-<主题>.md`，放在**仓库根目录**。
- **不进 `docs/`**：避免被 MkDocs 编进公开课程站（维护者视角文档）。
- 同一主题只维护一个 handoff 文件，多次触发为增量更新而非新建。

## 文档结构（强制章节）

```markdown
# Handoff — <主题>

> 创建时间：YYYY-MM-DD
> 最后更新：YYYY-MM-DD
> 状态：<一句话总览：进行中 / 部分阻塞 / 待用户决策>

## 1. 状态概览
<3–5 行说清当前整体进度>

## 2. 已完成
- <已交付、已验证的项>

## 3. 进行中
- <正在做、未完成的工作 + 当前卡点>

## 4. 阻塞 / 待决
- <需要用户/外部决策或依赖的事项>

## 5. 下一步行动（按优先级）
1. <可执行的具体步骤>
2. ...

## 6. 关键决策与用户偏好
- <本次确认的口径、约定（如"推 main 前先过目"）>

## 7. 相关文件与命令
- 文件：<路径>
- 命令：<git / mkdocs 等复现步骤>

## 8. 已知风险 / 注意事项
- <发布边界、私有依赖、强推风险等>
```

## 执行细则

1. **只动 handoff 文档**，不擅自修改业务/课件代码；如需改代码应另行确认或在 handoff 的"下一步"中登记。
2. **增量、不回退**：已标记为「已完成」的事实不得因本次扫描而抹除；新信号只追加或修正"进行中/阻塞/下一步"。
3. **偏好继承**：若既有 handoff 已记录用户偏好（如"推 `main` 前先贴 diff 过目"），本次更新须保留并沿用。
4. **推送守约**：handoff 更新后若需 commit/push，遵循用户约定——**推 `main` 前先贴变更给用户过目**，不自动推送。
5. **信号采集要全**：必须同时看 `git status`（未提交改动）、`git log`（近期脉络）、标记搜索（TODO 等），避免只看对话上下文导致遗漏。
6. **多个 handoff 冲突**：若根目录存在多个 `HANDOFF-*.md` 且主题不同，只更新与当前工作最相关的那个，并向用户说明其余文件未被触碰。

## 输出格式（回复用户）

```
## 📋 Handoff 更新完成

- 文件：HANDOFF-<主题>.md
- 本次增量：<1 行说明新增/修正了什么>
- 进行中：<N 项> ｜ 阻塞：<N 项> ｜ 待决：<N 项>
- 下一步 Top1：<最该先做的那一步>

（未推送，按约定待你确认后我再 commit/push）
```
